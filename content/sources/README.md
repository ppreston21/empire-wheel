# Source records

This directory is reserved for reusable, typed source ledgers once a module
needs records shared beyond its own content file. Records must follow
`SourceRecord` in `content/types.ts`, including access and verification dates.
Candidate research remains in `content/research/`; learner-facing uses must pass
the publication checks in `lib/publication.ts`.
